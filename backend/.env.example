# Server
PORT=4000

# Person 2 — Nebius Token Factory (OpenAI-compatible chat completions API)
NEBIUS_API_KEY=
NEBIUS_BASE_URL=https://api.tokenfactory.nebius.com/v1
NEBIUS_MODEL=Qwen/Qwen3-30B-A3B-Instruct-2507

# Person 3 — Cognee Cloud (patient memory). Optional: without these,
# memory.js still works fully via its in-memory store, just without
# cross-session/cross-deploy persistence.
COGNEE_API_KEY=
COGNEE_BASE_URL=

# Person 4 — Nimble (caregiver search)
NIMBLE_API_KEY=
